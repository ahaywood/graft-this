- [ ] Add the mergePrismaSchema.mjs file to the `src/scripts` directory
- [ ] Add a script to the `package.json` file to run the mergePrismaSchema.mjs file
    ```json
    "scripts": {
        "merge": "node src/scripts/mergePrismaSchema.mjs"
    }
    ```
